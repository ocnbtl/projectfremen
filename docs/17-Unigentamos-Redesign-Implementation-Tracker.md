# Unigentamos redesign implementation tracker

Current integration starting baseline: GitHub `main` at `5d0e9d7dbb9dd2b0ad07319541502d75d45b9449`. The live production endpoints were healthy at the 2026-08-06 pre-deployment gate; the next immutable Git and Vercel release identities are recorded after this cohort is integrated.

This is an implementation ledger, not a completeness claim. “Verified” means the route passed the isolated authenticated regression, production build, and its implemented responsive checks at the current checkpoint. “Bounded” means the route is usable for the stated read or preview workflow while its visible unavailable controls remain disabled with an explanation. Mockup values are never treated as live data.

| Target route | Source mockup / handoff | Canonical owner | Read path | Connected mutations | Migration / boundary | Desktop | Mobile | Accessibility / tests | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `/admin/people` | People final visual reference | People | Personal Records person adapter | Create/edit; explicit Personal Ops follow-up handoff | People keeps cadence; source-aware guard prevents accidental parallel active follow-ups | Verified | Verified route stack | Keyboard, focus, reload, URL state, duplicate handoff | Verified functional adapter |
| `/admin/people/new` | People Add Person | People | Person adapter defaults | Explicit create | Current protected API | Verified | Verified | Required fields and error retention | Verified functional adapter |
| `/admin/people/[personId]` | People profile | People | Person adapter | Edit entry point | Compact memories remain People-owned | Verified | Verified | Direct route and refresh | Verified functional adapter |
| `/admin/people/[personId]/edit` | People edit profile | People | Person adapter | Explicit save | Existing audit and CSRF path | Verified | Verified | Dirty-state protection | Verified functional adapter |
| `/admin/media` | Media All Media | Media | `file` Personal Records adapter plus Resource owner evidence | Title, description, review timing, and explicit source handoff to Resource | Handoff creates only the Resources-owned URL identity; Media and native link state stay unchanged | Verified | Verified detail route | Failure recovery, focus, URL state, 44 px targets | Verified functional adapter |
| `/admin/media/[assetId]` | Media asset detail | Media | Ownership-filtered Media adapter plus exact Resource, NoteLink, Project, Personal Ops, and ReviewRun matches | Title, description, review timing, explicit source handoff to Resource, Project association create/report/unlink/restore, and exact-source Review evidence repair/replacement handoff | Media stays source owner; Notes owns NoteLink lifecycle; Reviews owns evidence use, replacement review, run/link lifecycle, and audit; no duplicate asset, Note, ReviewRun, or `AssetReview` | Verified | Verified | Direct route, modal focus, failure retention, Note owner route, evidence-state refresh, reload, duplicate prevention, 44 px lifecycle targets | Verified functional adapter |
| `/admin/media/needs-review` | Media Needs Review | Media | Literal readiness evidence, legacy queue timing, and Resource owner evidence | Title, description, review timing, and explicit source handoff to Resource | Handoff does not complete review, confirm rights, mutate Media, or create an `AssetReview` | Verified | Verified | Evidence labels; no score; dirty protection; reload | Verified bounded workflow |
| `/admin/media/missing-metadata` | Media Missing Metadata | Media | Adapter evidence groups plus Resource owner evidence | Title, description, and explicit source handoff to Resource | Required native metadata and native linking remain unavailable | Verified | Verified | State labels; disabled reasons; handoff focus | Verified bounded workflow |
| `/admin/media/rights-usage` | Media Rights / Usage | Media | Literal rights/source evidence plus Resource owner evidence | Explicit source handoff to Resource | Canonical rights `needs_confirmation`; handoff never confirms rights or usage scope | Verified | Verified | Color-independent labels; handoff boundary | Verified bounded workflow |
| `/admin/media/duplicates` | Media Duplicates | Media | Exact safe source evidence | None | Never auto-merges or claims binary match | Verified | Verified | URL state and zero-mutation check | Verified evidence view |
| `/admin/media/in-use` | Media In Use | Media | Owner-module references | None | References are not `AssetUsage` | Verified | Verified | Focus containment and owner links | Verified evidence view |
| `/admin/media/upload-queue` | Media Upload Queue | Media | Local file metadata preview | Local draft only | No bytes, upload, browser persistence, or fake `RawFile` | Verified | Verified | Keyboard and explicit boundary | Verified local preview |
| `/admin/notes` | Notes directory / smart views | Notes | Authored-note adapter | Create/edit; routing fields; next-review date/cadence | Review timing is legacy-backed; lifecycle and review remain separate | Verified | Verified | URL history, failed-write retention, focus, 44 px targets | Verified functional adapter |
| `/admin/notes/[noteId]` | Notes editor and detail tabs | Notes | Note plus native Resource/Media NoteLinks, reference candidates, Projects-owned associations, and Reviews-owned ReviewRun evidence | Explicit body, routing-property, review-schedule saves; NoteLink create/promote/edit/health/repair/remove/restore; Project association lifecycle; exact-source ReviewRun handoff | Notes owns NoteLink relationship and audit state while Resource/Media keep target ownership; no citation, backlink, duplicate target, review completion, duplicate ReviewRun, waiver, or reviewer claim | Verified | Verified | CSRF, concurrency, reload, URL history, dirty/error retention, source isolation, owner routing, reduced motion, four-viewport lifecycle controls | Verified functional adapter |
| `/admin/resources` | Resources directory | Resources | Resource adapter and URL evidence | Create/edit; review timing; create Note draft; attach Note source evidence | URL identity stays Resource-owned; Notes owns authored body | Verified | Verified | Failure recovery, duplicate-source prevention, dirty protection | Verified functional adapter |
| `/admin/resources/[resourceId]` | Resource detail | Resources | Canonical resource plus exact legacy Note candidates, native Notes-owned NoteLinks, Projects-owned associations, and Reviews-owned Review evidence | Edit retained fields; explicit review timing; Notes handoff with duplicate-safe NoteLink; Project association lifecycle; exact-source stale/duplicate Review evidence repair handoff | Resource stays source owner; Notes owns NoteLink relationship/audit; Reviews owns evidence use, run/link lifecycle, and audit; native citation, extraction, snapshot, health, and Resource-local review remain bounded | Verified | Verified | Modal focus, failed/partial-write retention, exact owner routes, NoteLink refresh, back-navigation, audit evidence, 44 px lifecycle targets | Verified functional adapter |
| `/admin/projects` | Projects hub | Projects | Stable legacy projections plus native state | Explicit promotion | No generic task import | Verified | Verified shell | CSRF, idempotency, route continuity | Verified functional workflow |
| `/admin/projects/[projectId]` | Project detail tabs / Timeline | Projects | Native project repository plus exact Personal Ops Follow-up/Decision and ReviewRun owner references | Project, milestone, blocker, ProjectLink health/repair/lifecycle; source-aware Personal Ops handoffs; confirmation-only Reviews handoff | Projects owns milestones/blockers and relationship state; Personal Ops owns actionable Follow-ups and durable Decisions; Reviews owns ReviewRuns; project completion never rewrites linked owner lifecycles | Verified | Verified | CSRF, concurrency, exact-source duplicate suppression, due-date handoff, owner-route/back history, responsive status summaries, 44 px targets | Verified functional workflow |
| `/admin/personal` | Personal Ops Command | Personal Ops | Native schema-v2 view with v1 normalization | Native object writes | Current Goals behavior preserved | Verified | Verified shell | Protected route and explicit boundaries | Verified functional workflow |
| `/admin/personal/goals` | Personal Ops Goals | Personal Ops | Native Goals | Create and update | No silent formula | Verified | Verified shell | Concurrency and reload | Verified functional workflow |
| `/admin/personal/decisions` | Personal Ops Decisions | Personal Ops | Native Decisions plus legacy mappings and canonical Finance source references | Explicit conversion/update and source-preserving Finance handoff | Durable Decisions owned here; Finance remains the budget/close source owner | Verified | Verified shell | Idempotency, exact native source identity, reload, and provenance | Verified functional workflow |
| `/admin/personal/obligations` | Personal Ops Obligations | Personal Ops | Native Obligations | Create/update/complete | Completion requires evidence | Verified | Verified shell | Validation and audit | Verified functional workflow |
| `/admin/personal/follow-ups` | Personal Ops Follow-ups | Personal Ops | Native Follow-ups | Create/update/complete; confirmed separate-source override | People retains cadence context; exact active source duplicates fail closed | Verified | Verified shell | Outcome gate, server conflict, audited override, 44 px targets | Verified functional workflow |
| `/admin/personal/routines` | Personal Ops Routines | Personal Ops | Native Routines | CRUD, archive/restore, confirmed preview | Auto-create risk remains confirmation-gated | Verified | Verified shell | Idempotency and fail-closed tests | Verified functional workflow |
| `/admin/personal/inbox` | Personal Ops Capture Inbox | Personal Ops | Native Capture items | CRUD, split preview/confirm | Data-only navigation placement | Verified | Verified shell | Atomicity and provenance tests | Verified functional workflow |
| `/admin/personal/templates` | Personal Ops Templates | Personal Ops | Native Templates | CRUD, activate/use/archive/restore | Data-only navigation placement | Verified | Verified shell | Pure tests and fingerprint idempotency | Verified functional workflow |
| `/admin/personal/[slug]` | Personal Ops record detail | Personal Ops | Existing Personal Records | Existing safe writes | Compatibility route retained | Verified | Verified shell | Protected direct route | Verified compatibility route |
| `/admin/reviews` | Reviews Home | Reviews | Native `ReviewRun` plus legacy compatibility | Create/update/archive/restore; exact Project, Note, Resource, and Media context links; compatible Resource/Media evidence use; stale/broken context repair; stale/duplicate evidence repair; pending replacement review | Review coordination owns run, evidence use, replacement decision, and relationship state; handoff never copies or mutates the source object, and context alone never satisfies evidence | Verified | Verified route stack | Auth, concurrency, audit, duplicate suppression, replacement gate, repair rejection, exact URL restoration | Verified functional workflow |
| `/admin/reviews/[reviewId]` | ReviewRun detail | Reviews | Native ReviewRun plus route-aware Project, Personal Ops, Resource, and Media owner references | Checklist, evidence link/refresh/replace/review, carry-forward, context link health/repair, exact Follow-up reconciliation, complete | Completion rejects archived or unavailable Personal Ops Follow-up owners and unreviewed replacements; stale/duplicate evidence retains its source; completed/archived/canceled runs stay read-only; prior source identity remains auditable | Verified | Verified route stack | Weekly ten-check gate, evidence owner routing, stale/duplicate repair, replacement review, owner lifecycle restore/relink path, protected conflict, source reload persistence | Verified functional workflow |
| `/admin/reviews/weekly` | Weekly Reviews | Reviews | Native/legacy compatible index | Create and update | Compatibility retained | Verified | Verified shell | Route and persistence checks | Verified functional workflow |
| `/admin/reviews/weekly/[entryId]` | Weekly review detail | Reviews | Review adapter | Existing safe review updates | Native conversion explicit | Verified | Verified shell | Direct route | Verified compatibility workflow |
| `/admin/reviews/monthly` | Monthly Reviews | Reviews | Native monthly ReviewRun | Checklist/evidence state | Finance close stays Finance-owned | Verified | Verified shell | Thirteen checks plus external gate | Verified bounded workflow |
| `/admin/reviews/monthly/[entryId]` | Monthly review detail | Reviews | Review adapter | Existing safe review updates | Decisions readiness has nine checks | Verified | Verified shell | Explicit checks, no invented score | Verified bounded workflow |
| `/admin/finance` | Finance command view | Finance | Native persisted Finance state | Explicit create/import/archive/restore and linked owner handoffs | No payment or bank execution; mutations retain audit and idempotency receipts | Verified | Verified | Auth, CSRF, concurrency, failure recovery | Verified functional workflow |
| `/admin/finance/transactions` | Finance Transactions | Finance | Native ledger, transfer pairs, and import batches | Create, categorize, review, archive/restore, and confirmed CSV import | Server-held previews expire; raw CSV is not retained | Verified | Verified | Evidence gates, paired invariants, exact replay | Verified functional workflow |
| `/admin/finance/accounts` | Finance Accounts | Finance | Native accounts and cashflow | Create, archive, and restore | Restore reruns active-name uniqueness and balance constraints | Verified | Verified | Concurrency, lifecycle conflicts, chart summary | Verified functional workflow |
| `/admin/finance/bills` | Finance Bills | Finance | Native bills and payment-readiness state | Create and lifecycle updates | No payment execution or external bank integration | Verified | Verified | Bounded values and explicit queue state | Verified functional workflow |
| `/admin/finance/budgets` | Finance Budgets | Finance | Native budgets plus exact Personal Ops Decision status | Persisted budget changes and explicit Decision handoff | Finance owns the budget; Personal Ops owns the durable Decision | Verified | Verified | Invariants, source provenance, owner reload | Verified functional workflow |
| `/admin/finance/monthly-review` | Finance Monthly Review | Finance | Native close evidence plus exact Personal Ops Decision status | Close-check updates and explicit Decision handoff | Close remains blocked until its evidence gates pass; no financial execution | Verified | Verified | Explicit external gate and exact source provenance | Verified functional workflow |
| `/admin/finance/rules` | Finance Rules / Automation | Finance | Native persisted rule definitions | Create/edit/archive plus server-side pure rule tests | Rule tests never execute financial side effects | Verified | Verified | Pure evaluation, audit, and zero execution | Verified functional workflow |
| `/vault` | Local-first encrypted workspace | Vault | Encrypted IndexedDB plus Windows SQLite/files and opaque relay envelopes | Notes, contacts, resources, history, backup, and offline saves | Canonical imports use three-way field merge; relay and desktop storage are bounded | Verified | Verified offline shell | Browser setup, version history, encrypted backup, offline reload/save | Verified local-first workflow |

## Current checkpoint boundaries

- Authentication, authorization, session, CSRF, Supabase/RLS/service-role behavior, permanent navigation, shared shell, Current Goals, and global AI behavior are unchanged.
- Notes now persists a separate schema-v1 `note-links.json` state through the existing filesystem/Supabase `app_state` adapter. The protected `/api/notes/links` route canonicalizes every Note, Resource, and Media identity server-side, requires session and CSRF proof for mutations, applies optimistic concurrency, and records both NoteLink-local and existing request audit events.
- Exact Note-to-Resource/Media creation is idempotent. Relationship/context edits, stale or broken reports with required explanations, repair/relink with prior-target evidence, soft removal with reason, and restoration retain one stable relationship ID and never mutate or copy either native owner object.
- Notes direct URLs restore the selected relationship through `?tab=links&link=<id>`. Resources Notes and Media Links read the exact Notes-owned lifecycle with last-known-state retention on refresh failure and route back to the owner row; removed and unhealthy links remain visible rather than disappearing.
- Resource-to-Note creation/attachment now completes the legacy source-evidence save and a separate Notes-owned `source` NoteLink. A Note save followed by NoteLink failure is reported as partial success, keeps the saved Note, and offers an idempotent relationship retry without claiming a citation or extraction.
- The isolated NoteLink browser gate covers 1920Ã—1080, 1440Ã—900, 1024Ã—768, and 390Ã—844 with direct selection, back/forward restoration, reduced motion, zero read-only mutations, no horizontal overflow, no refresh/AI-dock collision, and 44 px mobile controls.
- Every route now emits the shared `nosniff`, `DENY` frame, strict-origin referrer, and camera/microphone/geolocation permissions-policy baseline; the regression verifies these headers on the login boundary without changing session or authorization behavior.
- Vercel Analytics now renders only in the Vercel runtime (`VERCEL=1`), preserving production telemetry while preventing isolated local verification servers from requesting a Vercel-only script with an invalid MIME type.
- The release dependency graph is hardened on Next.js 16.2.12 with explicit PostCSS 8.5.25 and Sharp 0.35.3 overrides. A clean `npm ci`, full audit, production build, and isolated regression all pass on the resolved tree.
- Notes, Resources, and Media now expose the lifecycle of their exact Projects-owned `ProjectLink` records. Each source can create, soft-unlink with a required reason, retry without losing that reason, and restore the same association; the source object and destination Project are never duplicated or deleted.
- Notes, Resources, and Media can report an association as stale, broken, or missing only with an observed-issue explanation. Broken and missing links retain context, disable unsafe source opening, and route to the exact Projects-owned row for repair instead of creating a replacement source object.
- Projects repairs an unhealthy `ProjectLink` only after a verified native source and repair explanation are supplied through the protected API. The stable relationship ID is retained, the previous source identity remains in `lastRepair` and audit history, duplicate exact source/relationship repair fails closed, and restoring a removed link returns to its pre-removal health state.
- Shared Project-state consumers reconcile through the protected read path on mount and after return navigation. Failed refreshes retain last-known state with an explicit error; the source-to-owner-to-source browser loop proves repaired state and provenance survive back navigation.
- Projects Files & Links keeps long native-source identifiers readable on mobile by stacking source content above its action group. Shared detail tabs keep the active tab fully visible and separate count badges from their labels instead of compressing operational text.
- Removed Project associations remain visible with their relationship, strength, required-evidence flag, project context, removal reason, and owner-module route. Missing targets retain a repair route, while completed or archived Projects explain why lifecycle mutations are unavailable. The existing protected Projects API continues to enforce CSRF, optimistic concurrency, audit, and timeline events.
- People, Projects, Notes, and Reviews source handoffs now reuse one source-aware Personal Ops Follow-up guard. An existing active Follow-up is shown before creation, save stays disabled until a separate outcome is explicitly confirmed, and the protected store independently rejects unconfirmed duplicates under its mutation lock.
- Projects now reads explicit native ReviewRun context for the Project and its milestone/blocker children. Review lifecycle, current state, completion blockers, link health, and the canonical owner route are visible in Overview, Timeline, selected-child context, and the completion rail without creating a Project-owned Review field or `ProjectLink`.
- Project Timeline rows and selected milestone/blocker inspectors now reconcile exact Personal Ops-owned Follow-up and Decision state beside Review coverage. Handoffs preserve the child source identity, parent Project ID, canonical owner route, and due date; once an exact Decision owner exists, duplicate creation actions disappear.
- Project completion surfaces active Personal Ops follow-through and unresolved durable Decisions as owner-controlled context. These counts never become hidden project fields or automatic completion gates, and completing a milestone or Project never closes or rewrites the Personal Ops or Reviews lifecycle.
- The Project-to-Reviews handoff serializes only the stable Projects source identity. Reviews validates the bounded source type, reconstructs the canonical owner route, requires the user to select a native ReviewRun and confirm through the existing CSRF/concurrency/audit mutation, and restores an existing soft-removed context link instead of creating a duplicate.
- Reviews now distinguishes linked, stale, broken, and removed context without deleting history. Stale and broken links retain their last-known identity and a required observed-issue reason; repair requires explicit source confirmation and a reason, preserves the stable relationship ID, records the prior reference, and rejects replacement with a source already linked to the same ReviewRun.
- Projects routes stale or broken Review coverage directly to the exact Reviews-owned context row. The Project handoff recognizes an unhealthy existing relationship and offers repair instead of duplicate creation; healthy, completed, archived, and canceled ReviewRun behavior remains unchanged.
- Long shared confirmation sheets now open at the top with a visible safe-dismiss focus target and keep their header and action rail available while content scrolls. The Review repair browser gate asserts the geometry before accepting the visual receipt.
- Review refresh failures preserve last-known Project context with an explicit retry. The isolated browser gate proves exact-source idempotency, soft unlink/relink, stale/broken/repair persistence, duplicate-repair rejection, no Project identity/link mutation, direct reload, back navigation, unrelated URL-state retention, canonical owner routes, no document overflow, and 44 px mobile targets at 1920×1080, 1440×900, 1024×768, and 390×844.
- Notes, Resources, and Media now read the current status of ReviewRuns that reference their exact native source identity. Each source shows Reviews-owned cadence, lifecycle, completion blockers, link health, and the exact owner row while retaining its separate local readiness or review-timing semantics.
- The existing Reviews handoff now accepts an allowlisted Note, Resource, or Media source as well as the compatible Project context handoff. Selecting the owning ReviewRun and confirming uses the existing protected CSRF, optimistic-concurrency, duplicate-safe, soft-lifecycle, and audit contracts; the source record is never copied or mutated.
- Source-side ReviewRun refresh failures preserve last-known context with a recoverable error. Direct reload, canonical owner and handoff routes, URL state, settled tablet overlays, no horizontal overflow, and 44 px mobile targets are verified at 1920×1080, 1440×900, 1024×768, and 390×844.
- Weekly and monthly ReviewRun template version 2 add optional, nonblocking Resource evidence requirements for their existing resource-cleanup checks. Historical version 1 runs continue to resolve against their original definitions, so waivers and completion semantics do not drift after the template update.
- Resource and Media handoffs now distinguish exact Reviews-owned evidence use from context-only linkage. Compatible unresolved requirements can be opened with the exact source prefilled in the existing evidence editor; the protected save path retains CSRF, optimistic concurrency, audit, source allowlisting, and duplicate-safe context behavior.
- Source modules route exact evidence use to the owning ReviewRun evidence row and context-only linkage to the owning overview row. A visible boundary explains that context alone never satisfies an evidence requirement, and no Resource, Media, Note, ReviewRun, or duplicate native evidence object is created by opening the handoff.
- Stale and duplicate Review evidence now retains its exact last-known Resource or Media source identity. Source modules show the Reviews-owned state and route to the exact evidence row; the handoff offers repair through the existing protected mutation instead of creating a second evidence record or rewriting source-module readiness.
- Changing an evidence source records a pending replacement with its reason and previous source reference. The replacement stays visibly unresolved until an explicit second review confirms it; the current ledger, source handoff, completion counts, API audit event, and reload state all use the same Reviews-owned lifecycle.
- Intentional parallel Follow-ups keep independent IDs and state but share the exact native source reference. The transient confirmation is not stored as domain data; the resulting creation audit uses `follow_up.created_with_duplicate_source_confirmation`. Completed or archived work does not block the next cadence instance.
- Date-only handoff values remain the exact selected calendar day instead of passing through UTC parsing. The browser gate asserts the retained date at desktop, tablet, and mobile sizes.
- Notes review timing now supports an explicit next-review date plus one-time, weekly, monthly, quarterly, annual, or adapter-compatible custom cadence. The same sheet opens from the directory Review inspector, full Review tab, full Properties tab, and editor header; failed writes preserve the draft and schedule removal requires consequence confirmation.
- Scheduling updates only `time.nextReview` and `time.reviewCadence` on the existing legacy Personal Record. The derived `scheduled` / `needs_review` state and queue count refresh from the saved date; no background scheduler, reviewer, blocker resolution, waiver, review completion, or Reviews-owned `ReviewRun` is created.
- Notes now permits explicit editing of the three legacy routing arrays the protected adapter already round-trips: Areas, Subjects, and legacy project labels. The same audited PATCH is available from the full Properties tab and Missing Properties inspector; title, body, lifecycle, review, privacy, sources, relations, and cross-module objects remain untouched.
- Notes project labels are retrieval metadata only. They never create or imply a Projects-owned native link. Native owner, type remapping, pinned state, review completion, body versions, inferred backlinks, schema migration, Note archive, and a unified Note audit reader remain intentionally unavailable.
- Resources can now create one private authored Note draft or append its safe HTTP(S) URL to one selected existing Note through the protected Notes adapter. The workflow never copies the Resource body automatically, preserves existing Note title/body/lifecycle/review fields, blocks duplicate normalized source attachment, and leaves the Resource record unchanged.
- Resources review timing now supports an explicit next-review date plus manual/one-time, weekly, monthly, quarterly, or annual cadence through the protected Personal Records PATCH route. The same sheet opens from Overview and Review, failed writes preserve the draft, unknown legacy intervals are preserved until explicitly changed, and schedule removal requires consequence confirmation.
- Resource Properties now exposes the protected retained-field editor, legacy review scheduler, exact Notes handoff, and Projects-owned association sheet from the approved control-plane surface. Unsupported owner, lifecycle, citation-policy, health, automation, archive, replacement, and merge controls remain explicit read-only previews; opening the supported workflows creates no duplicate Resource, Note, or Project-owned object.
- Resource timing updates only `time.nextReview` and `time.reviewCadence`; they do not mark any of the nine review contracts complete, infer a reviewer, fetch the URL, create a ReviewRun or review event, update citations, attach a Media snapshot, change lifecycle, or schedule background work.
- Resource → Note handoff stores legacy `externalSources` evidence and one Notes-owned source `NoteLink`. It does not claim a native citation, extraction anchor, source-use decision, fetched snapshot, or Note-body insertion.
- Media now permits the unambiguous legacy-backed title, description, next-review date, and review-cadence fields. Both sheets update the existing `file` record through the protected Personal Records route and its existing audit event; they do not create a Media mega-record or a duplicate object.
- Media review timing is queue planning only. It supports next-review sorting plus explicit schedule/edit/remove flows from canonical Review detail and Needs Review, preserves failed and dirty input, and never creates or completes an `AssetReview`, assigns a reviewer, satisfies readiness evidence, confirms rights, creates a Reviews-owned `ReviewRun`, or starts background work.
- Media source candidates can now be explicitly handed to Resources from canonical Media detail and the Needs Review, Missing Metadata, and Rights / Usage inspectors. The fixed accepted HTTP(S) URL and editable user title/context create one private active Resource through the existing protected adapter; exact matches reopen the owner route instead of offering duplicate creation.
- Media → Resources handoff never mutates the Media record, confirms its source, changes rights/readiness/review state, creates a binary, or claims a persisted `ObjectLink`. Until native link persistence is resolved, the UI reports the exact owner match and keeps the relationship visibly pending.
- Production Finance now uses native persisted state with authenticated, CSRF-protected, optimistic-concurrency mutations, semantic idempotency receipts, audit events, invariant checks, bounded import previews, and reversible archive/restore. It does not connect to banks, execute payments, or retain raw CSV.
- Finance budget and close-check Decision handoffs preserve their exact canonical Finance source reference through Personal Ops creation and isolated reload. The protected Decision write remains explicit and never executes a Finance transaction or close automatically.
- Review Follow-up reconciliation now considers only non-archived Personal Ops owners. A linked archived or unavailable owner remains visible with its canonical route and explicit restore, replacement, or relink actions; the completion rail reports the lifecycle blocker instead of treating the stored reference as sufficient.
- The protected ReviewRun API rechecks every linked Personal Ops Follow-up before accepting a link or Review completion. Missing or archived owners fail with a recoverable conflict after optimistic-concurrency validation, while CSRF, audit, outcome gates, soft archive, and module-native ownership remain unchanged.
- Native Media persistence topology, binary storage, retention, source confirmation, ownership, accessibility/OCR, rights confirmation, review completion, lifecycle, versions, derivatives, replacement, native links, usage, and AI execution remain intentionally unavailable.
- External Docs sync and Sentry sync are excluded from the default isolated regression because they require third-party network actions.
- The current isolated regression completes 131 checks with only the two documented external Docs/Sentry sync skips.

## 2026-08-09 vault durability checkpoint

- The encrypted relay now has acknowledgement-gated semantic compaction. Cleanup is serialized per vault, runs only after every active device has caught up with no queued or blocked changes, preserves the first envelope, a 256-envelope recovery window, every meaningful local version, and every current object version, and writes a server-only receipt.
- Vault devices now have an explicit active or retired lifecycle. Removing an old device is a two-step action, cannot remove the current device, does not remotely erase the retired device, prevents the retired browser from resuming relay writes, and keeps retired devices out of the compaction safety cursor.
- The Vault health panel reports per-browser offline-storage protection and approximate usage, encrypted relay rows and bytes, cleanup state, Windows backup destination, last verified backup, schedule, capacity, and actionable warnings in direct language.
- The Windows companion now schedules signed, verified encrypted backups every seven days by default while unlocked. It never silently deletes an older recovery set when the configured limit is full, and the installer exposes `-AutoBackupDays` for an intentional cadence change.
- The implementation retains the existing authenticated session, CSRF, RLS, encrypted-envelope, optimistic merge, conflict-history, bounded-media, and additive-restore boundaries. The rollout is additive and does not mutate production user records or connect to payments, banks, or paid providers.